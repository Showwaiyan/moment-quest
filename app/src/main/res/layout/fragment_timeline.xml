<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background">

    <!-- Filters Container -->
    <LinearLayout
        android:id="@+id/filtersContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="8dp"
        app:layout_constraintTop_toTopOf="parent">

        <!-- Category Scroll View -->
        <HorizontalScrollView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:scrollbars="none">

            <com.google.android.material.chip.ChipGroup
                android:id="@+id/chipGroupCategory"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                app:singleSelection="true"
                app:selectionRequired="true">

                <com.google.android.material.chip.Chip
                    android:id="@+id/chipCatAll"
                    style="@style/Widget.Material3.Chip.Filter"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="All"
                    android:checked="true"
                    app:chipCornerRadius="12dp"
                    app:chipBackgroundColor="@color/category_chip_background"
                    app:chipStrokeColor="@color/category_chip_stroke"
                    app:chipStrokeWidth="1dp"
                    android:textColor="@color/category_chip_text"
                    app:checkedIconVisible="false" />

                <com.google.android.material.chip.Chip
                    android:id="@+id/chipCatTravel"
                    style="@style/Widget.Material3.Chip.Filter"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Travel"
                    app:chipCornerRadius="12dp"
                    app:chipBackgroundColor="@color/category_chip_background"
                    app:chipStrokeColor="@color/category_chip_stroke"
                    app:chipStrokeWidth="1dp"
                    android:textColor="@color/category_chip_text"
                    app:checkedIconVisible="false" />

                <com.google.android.material.chip.Chip
                    android:id="@+id/chipCatLearning"
                    style="@style/Widget.Material3.Chip.Filter"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Learning"
                    app:chipCornerRadius="12dp"
                    app:chipBackgroundColor="@color/category_chip_background"
                    app:chipStrokeColor="@color/category_chip_stroke"
                    app:chipStrokeWidth="1dp"
                    android:textColor="@color/category_chip_text"
                    app:checkedIconVisible="false" />

                <com.google.android.material.chip.Chip
                    android:id="@+id/chipCatFitness"
                    style="@style/Widget.Material3.Chip.Filter"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Fitness"
                    app:chipCornerRadius="12dp"
                    app:chipBackgroundColor="@color/category_chip_background"
                    app:chipStrokeColor="@color/category_chip_stroke"
                    app:chipStrokeWidth="1dp"
                    android:textColor="@color/category_chip_text"
                    app:checkedIconVisible="false" />

                <com.google.android.material.chip.Chip
                    android:id="@+id/chipCatSocial"
                    style="@style/Widget.Material3.Chip.Filter"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Social"
                    app:chipCornerRadius="12dp"
                    app:chipBackgroundColor="@color/category_chip_background"
                    app:chipStrokeColor="@color/category_chip_stroke"
                    app:chipStrokeWidth="1dp"
                    android:textColor="@color/category_chip_text"
                    app:checkedIconVisible="false" />
            </com.google.android.material.chip.ChipGroup>
        </HorizontalScrollView>

        <!-- Status Scroll View -->
        <HorizontalScrollView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:scrollbars="none">

            <com.google.android.material.chip.ChipGroup
                android:id="@+id/chipGroupStatus"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                app:singleSelection="true"
                app:selectionRequired="true">

                <com.google.android.material.chip.Chip
                    android:id="@+id/chipStatusAll"
                    style="@style/Widget.Material3.Chip.Filter"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="ALL"
                    android:checked="true"
                    app:chipCornerRadius="12dp"
                    app:chipBackgroundColor="@color/status_chip_background"
                    app:chipStrokeColor="@color/status_chip_stroke"
                    app:chipStrokeWidth="1dp"
                    android:textColor="@color/status_chip_text"
                    app:checkedIconVisible="false" />

                <com.google.android.material.chip.Chip
                    android:id="@+id/chipStatusPending"
                    style="@style/Widget.Material3.Chip.Filter"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="PENDING"
                    app:chipCornerRadius="12dp"
                    app:chipBackgroundColor="@color/status_chip_background"
                    app:chipStrokeColor="@color/status_chip_stroke"
                    app:chipStrokeWidth="1dp"
                    android:textColor="@color/status_chip_text"
                    app:checkedIconVisible="false" />

                <com.google.android.material.chip.Chip
                    android:id="@+id/chipStatusCompleted"
                    style="@style/Widget.Material3.Chip.Filter"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="COMPLETED"
                    app:chipCornerRadius="12dp"
                    app:chipBackgroundColor="@color/status_chip_background"
                    app:chipStrokeColor="@color/status_chip_stroke"
                    app:chipStrokeWidth="1dp"
                    android:textColor="@color/status_chip_text"
                    app:checkedIconVisible="false" />
            </com.google.android.material.chip.ChipGroup>
        </HorizontalScrollView>
    </LinearLayout>

    <!-- RecyclerView for Feed -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewTimeline"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:paddingHorizontal="16dp"
        android:clipToPadding="false"
        app:layout_constraintTop_toBottomOf="@id/filtersContainer"
        app:layout_constraintBottom_toBottomOf="parent"/>

    <!-- Loading Indicator -->
    <ProgressBar
        android:id="@+id/progressBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Empty/Error State -->
    <TextView
        android:id="@+id/tvEmptyState"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Your timeline is empty. Capture a moment or set a challenge!"
        android:textColor="@color/on_surface_variant"
        android:fontFamily="@font/be_vietnam_pro"
        android:textSize="14sp"
        android:gravity="center"
        android:padding="32dp"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
